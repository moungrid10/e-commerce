
enum ProductType {single, variable}

enum TextSizes {small, meduim, large}

enum OrederStatus {processing, shipped, delivred,pending} 

enum PaymentMethods {paypal, googlePay, applePay, visa, mastercard, CreditCard}
